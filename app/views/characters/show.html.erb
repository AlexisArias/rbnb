<div class="container">
  <%= link_to "Retour à la liste", root_path, :class => "btn btn-outline-dark btn-rounded mt-3 mb-4"%>

  <div class="d-flex border img-border shadow-sm p-4">
    <%= image_tag @character.image, class:"d-inline-block img-border", style: "width: 300px; height: 400px; object-fit: cover;"%>
    <div class="d-flex flex-column m-4 justify-content-between">
      <div class="d-flex justify-content-between align-items-end mb-4 border-bottom border-1">
        <h1><strong><%= @character.name %></strong></h1>
        <p>2/5 <% @character.rating.to_i.times do  %>
        <i class="bi bi-star-fill" style="color:orange;"></i>
        <% end %>
        </p>
      </div>
      <h5 class="mt-5"><strong>Description du personnage</strong></h5>
      <p class="salut"><%= @character.description %></p>
      <div data-controller="toggle" class="mt-5" style="position:relative">
        <button data-action="click->toggle#fire" class="btn btn-dark">Louer ce personnage</button>
          <div data-toggle-target="togglableElement" class="d-none">
            <div class="border text-dark bg-white p-4" style="width:300px;height:270px;position:absolute;top:-3px;left:220px">
            <%= render "form", rental: @rental %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="mt-5 pb-3 mb-4 d-flex justify-content-between align-items-center border-bottom border-1">
    <div>
      <h4>Louez plus.</h4>
      <p>Elle est où la poulette ?</p>
    </div>
    <%= link_to "Voir tous nos personnages", root_path, :class => "btn btn-outline-dark btn-rounded mt-3 mb-4"%>
  </div>
  <div class="d-flex justify-content-between scrollmenu">
    <div class="children d-flex justify-content-between align-items-center border img-border shadow-sm p-4 m-1" style="width:250px">
        <%= image_tag Character.find(97).image, class:"d-inline-block img-border", style:"width: 100px; height: 150px; object-fit: cover; max-width:200px;"%>
        <div class="d-flex flex-column align-items-center">
          <p class="m-0"><strong><%= Character.find(97).name %></strong></p>
          <p><% Character.find(97).rating.to_i.times do  %>
            <i class="bi bi-star-fill" style="color:orange;"></i>
          <% end %>
          </p>
          <%= link_to "Louer", character_path(Character.find(97)), :class => "btn btn-dark btn-rounded"%>
        </div>
    </div>
    <div class="children d-flex justify-content-between align-items-center border img-border shadow-sm p-4 m-1" style="width:250px">
        <%= image_tag Character.find(99).image, class:"d-inline-block img-border", style:"width: 100px; height: 150px; object-fit: cover; max-width:200px;"%>
        <div class="d-flex flex-column align-items-center">
          <p class="m-0"><strong><%= Character.find(99).name %></strong></p>
          <p><% Character.find(99).rating.to_i.times do  %>
            <i class="bi bi-star-fill" style="color:orange;"></i>
          <% end %>
          </p>
          <%= link_to "Louer", character_path(Character.find(99)), :class => "btn btn-dark btn-rounded"%>
        </div>
    </div>
    <div class="children d-flex justify-content-between align-items-center border img-border shadow-sm p-4 m-1" style="width:250px">
      <%= image_tag Character.find(100).image, class:"d-inline-block img-border", style:"width: 100px; height: 150px; object-fit: cover; max-width:200px;"%>
      <div class="d-flex flex-column align-items-center">
        <p class="m-0"><strong><%= Character.find(100).name %></strong></p>
        <p><% Character.find(100).rating.to_i.times do  %>
          <i class="bi bi-star-fill" style="color:orange;"></i>
        <% end %>
        </p>
          <%= link_to "Louer", character_path(Character.find(100)), :class => "btn btn-dark btn-rounded"%>
      </div>
    </div>
    <div class="children d-flex justify-content-between align-items-center border img-border shadow-sm p-4 m-1" style="width:250px">
      <%= image_tag Character.find(101).image, class:"d-inline-block img-border", style:"width: 100px; height: 150px; object-fit: cover; max-width:200px;"%>
      <div class="d-flex flex-column align-items-center">
        <p class="m-0"><strong><%= Character.find(101).name %></strong></p>
        <p><% Character.find(101).rating.to_i.times do  %>
          <i class="bi bi-star-fill" style="color:orange;"></i>
        <% end %>
        </p>
        <%= link_to "Louer", character_path(Character.find(101)), :class => "btn btn-dark btn-rounded"%>
      </div>
    </div>
  </div>

</div>
